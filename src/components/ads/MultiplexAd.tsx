/**
 * Multiplex / "Sponsored Links" units removed — they reserved large empty
 * space when unfilled and conflicted with AdSense Auto ads.
 * Component kept as a no-op so existing imports stay valid.
 */
export default function MultiplexAd() {
  return null;
}
