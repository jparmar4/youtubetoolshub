/**
 * Keep this component as a no-op so existing imports stay stable.
 *
 * The homepage already includes visible FAQ and key-fact sections. Duplicating
 * large blocks of hidden, search-only text can look manipulative to crawlers and
 * is less useful than content visitors can actually read.
 */
export default function AnswerBoxes() {
  return null;
}
